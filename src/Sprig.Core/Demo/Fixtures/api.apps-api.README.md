# apps/api

A placeholder monorepo slice, here so the "Split a repo into modules" learning guide can point a
module at a real subdirectory (and show the green ✓ that a path exists). In a real repo this folder
would hold the api service's own code, its `.env` files, its compose file and its setup commands —
everything a module owns — while the repo's inputs stay shared above.
