# Committed template — safe to keep in git because it holds no real values.
#
# sprig seeds each worktree's .env from this file, then clobbers exactly the keys the
# stack supplies (PORT, DATABASE_URL). Everything else below is passed through untouched.
APP_NAME=sample-api
LOG_LEVEL=debug
PORT=5000
DATABASE_URL=postgres://sample:sample@localhost:5432/sample
