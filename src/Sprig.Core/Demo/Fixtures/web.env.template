# Committed template. sprig seeds each worktree's .env from here, then clobbers PORT (this
# repo's own web port) and VITE_API_URL (wired from the api capability the map provides).
APP_NAME=sample-web
PORT=3000
VITE_API_URL=http://localhost:5000
