# Committed template. sprig seeds each worktree's .env from here, then clobbers PORT and
# VITE_API_URL with the values its stack supplies for that workspace.
APP_NAME=sample-web
PORT=3000
VITE_API_URL=http://localhost:5000
